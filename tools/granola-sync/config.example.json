{
  "_comment": "config.json is OPTIONAL. The daemon auto-discovers serverUrl + bearerToken from ~/.claude.json. Defaults below match the Dent install. Override any field to retarget the daemon at a different org.",
  "orgKeywords": ["dent"],
  "orgDomains": ["dentthefuture.com"],
  "orgFolders": ["Dent"],
  "fileAll": false,
  "granolaCachePath": "${HOME}/Library/Application Support/Granola/cache-v6.json",
  "cursorPath": "${HOME}/.dent-brain/granola-sync/cursor.json"
}
