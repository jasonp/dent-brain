{
  "_comment": "config.json is OPTIONAL. The daemon auto-discovers serverUrl + bearerToken from ~/.claude.json and reads the Granola API key from the macOS keychain (service: dent-brain.granola-sync). Defaults below match the Dent install. Override any field to retarget the daemon at a different org.",
  "orgKeywords": ["dent"],
  "orgDomains": ["dentthefuture.com"],
  "orgFolders": ["Dent"],
  "fileAll": false,
  "cursorPath": "${HOME}/.dent-brain/granola-sync/cursor.json"
}
