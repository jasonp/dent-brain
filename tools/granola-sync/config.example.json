{
  "_comment": "config.json is OPTIONAL. The daemon auto-discovers serverUrl + bearerToken from ~/.claude.json (set by `claude mcp add dent-brain ...` during /dent-onboard-teammate). dentDomains defaults to ['dentthefuture.com']. Override anything here only if you need to.",
  "dentDomains": ["dentthefuture.com"],
  "granolaCachePath": "${HOME}/Library/Application Support/Granola/cache-v6.json",
  "cursorPath": "${HOME}/.dent-brain/granola-sync/cursor.json"
}
